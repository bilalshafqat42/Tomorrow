/** @type {import('next').NextConfig} */
const nextConfig = {
  // It's standard practice to keep this enabled for better debugging and future compatibility.
  reactStrictMode: true,

  // IMPORTANT: All other path-related settings (like basePath, assetPrefix, etc.)
  // are omitted to ensure default root-relative path resolution.
};

export default nextConfig;
